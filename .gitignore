# Runtime data (generated, not source)
peers.json
geo_cache.json
zone_scan.json
zone_scan_state.json
feed_cache.json
stake_cache.json
telemetry_cache.json
crawler.log
publish.log
zone-scanner.log
# Rotated/compressed logs
*.log.gz
*.log.*.gz

# Local ops scripts (machine-specific, not part of the app)
disk-indicator.py
disk-watch.sh

# Python
.venv/
__pycache__/
*.pyc

# Rust build artifacts
crawler/target/
zone-scanner/target/

# Pages worktree (managed separately)
pages/

# Debug dir
debug/

# Fieldcraft symlinks (machine-local — run scripts/setup-fieldcraft.sh to recreate)
.claude/rules/
